import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

// Wrap the App component in React.StrictMode if needed
ReactDOM.createRoot(document.getElementById('root')).render(
  <App />
)
